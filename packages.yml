packages:
  # this package pins dbt_utils v0.6.3 (for use with dbt Core version <0.19.0)
  - git: "https://github.com/matt-winkler/cross-project-docs-source.git"
    revision: 1.0.2

  # adding this will break deps due to a version conflict
  #- package: fishtown-analytics/dbt_utils
  #  version: 0.6.1